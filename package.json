{
  "name": "transpiler",
  "version": "0.0.1",
  "description": "Transpile TypeDoc JSON output to GenMyModel XML",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stefan & Flippie",
  "dependencies": {
    "xml-writer": "^1.6.0"
  }
}
